XGBoost
AdaBoost 与 XGBoost 同属集成学习中的 Boosting 框架，但**提升机制与训练逻辑完全不同**：
**AdaBoost 通过迭代提高错分样本的权重，让后续弱分类器专注攻克难以分类的样本，最终以加权投票得到结果，
更侧重对困难样本的关注，但易过拟合且对噪声敏感**；
**XGBoost 则采用梯度提升策略，每一轮训练都拟合上一轮预测产生的负梯度（残差），
通过逐步修正预测误差不断逼近真实标签，输出为所有树的加权和，并自带结构正则化与二阶泰勒展开优化，
拟合能力更强、泛化性更稳定，适用于高维复杂医学数据建模**。
